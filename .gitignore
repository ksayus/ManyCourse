# ══════════════════════════════════════════════════════════════════════════
#  这个仓库只放**跑起来 App 所必需的东西**：
#
#    ① 源码与工程配置
#       app/src/main/ 、gradle/ 、build.gradle.kts 、settings.gradle.kts 、gradlew*
#    ② 测试
#       app/src/test/（JVM 单测）、app/src/androidTest/（真机测试）
#    ③ 开发脚本
#       tools/（抓包摘要、CAS 联调、TGT 有效期观测、UI 树读取等验证工具）
#
#  判断标准就一条：**这份东西是不是"跑起来 App 所必需的"？**
#    - 是   → 提交（源码、Gradle 配置、wrapper、测试、开发脚本）
#    - 不是 → 不提交（构建产物、本机配置、签名密钥、IDE 状态、抓包、开发文档、设计素材）
#      其中构建产物与 IDE 状态还能自动重建；开发文档与设计素材属于
#      "重建不了、但也不打算放进仓库"的那一类，留在本机即可。
#
#  下面每一段都是"某样东西为什么不进仓库"，加规则时请连理由一起写。
# ══════════════════════════════════════════════════════════════════════════

# ── 构建产物 ────────────────────────────────────────────────────────────
# 编译/打包结果：换台机器 build 一次就有，而且 diff 全是二进制，review 不了
# （模块内的 app/build 由 app/.gitignore 负责）
/build
/app/release/
*.apk
*.aab
*.ap_
*.dex
*.hprof
/captures
.externalNativeBuild
.cxx

# ── 本机配置与签名密钥 ★ 绝对不能提交 ──────────────────────────────────
# local.properties 里是**这台机器**的 SDK 路径，别人 clone 下来必须自己写一份；
# *.jks / *.keystore 是签名密钥 —— 提交上去等于把"能发布这个 App"的能力也交出去。
local.properties
*.jks
*.keystore
keystore.properties
signing.properties

# ── IDE / 工具链状态 ────────────────────────────────────────────────────
# Android Studio 打开工程时会自己重建；提交了只会天天产生无意义 diff
# （尤其是 .idea/workspace.xml，全是"我刚点了哪个文件"这种本机状态）
.idea/
*.iml
.gradle/
.kotlin/
.DS_Store
Thumbs.db
*.log

# ── 抓包导出 ────────────────────────────────────────────────────────────
# HAR 是**完整会话记录**：姓名、学号、班级、课表、Cookie 全在里面，
# 属于个人信息，只在本机拿来核对接口用
/web_fetch

# ── 开发文档 ────────────────────────────────────────────────────────────
# docs/ 下是**开发过程材料**：架构与实现指南、UI 使用文档、教务接口接入指南、
# 会话记录 —— 记录"这东西是怎么做出来的"，本身不是 App 的一部分，也不参与构建。
# 要给别人看的话单独发文件，不进仓库。
/docs/

# ── 与工程无关的素材 ────────────────────────────────────────────────────
# 根目录这两张是 UI 效果图，app/ 里没有任何引用
/v2_portrait.png
/v2_restore.png
# campus-schedule 是设计系统的一次性导出（HTML/CSS/JSON 预览页 + SKILL.md），
# 既不参与编译、也没被 app/ 引用 —— 设计稿留在本地看就行
/campus-schedule/

# ── 图标源文件 ──────────────────────────────────────────────────────────
# tools/icon/ 放设计给的原始 .ico（里面是 16～256px 七张 PNG）。App 编译用的是
# tools/make_launcher_icons.ps1 从它生成的那批 PNG（在 res/ 里，已提交），
# .ico 本身不参与构建，属于上面说的"设计素材"。
# 换图标：把新 .ico 丢进 tools/icon/，跑一次 pwsh tools/make_launcher_icons.ps1。
/tools/icon/
